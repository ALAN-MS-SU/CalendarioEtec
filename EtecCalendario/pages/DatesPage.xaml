<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EtecCalendario.pages.DatesPage"
             Title="DatesPage">
    <Grid RowDefinitions=".6*,8*,1*" ColumnDefinitions=".6*,1*,1*">
        <Button Grid.Row="0" Grid.Column="0" x:Name="Back" Clicked="Back_Clicked" Style="{StaticResource Button}" Text="Voltar" FontSize="Default" WidthRequest="70" HeightRequest="45" HorizontalOptions="Start" Margin="10,0,0,0" />
        <Label Grid.Row="0" VerticalOptions="Center" Grid.Column="1" FontSize="Medium" Text="{Binding Class,StringFormat='Turma: {0}'}"   Style="{StaticResource PrimaryTitle}"/>
        <Label Grid.Row="0" VerticalOptions="Center" Grid.Column="2" FontSize="Medium" Text="{Binding Subject,StringFormat='Matéria: {0}'}"   Style="{StaticResource PrimaryTitle}"/>
        <Button Grid.Row="2" Grid.Column="3" Clicked="Button_Clicked" HorizontalOptions="End" AbsoluteLayout.LayoutBounds="10,100" AbsoluteLayout.LayoutFlags="None" Style="{StaticResource Button}" WidthRequest="70" Text="+" FontSize="40" FontAttributes="Bold" CornerRadius="50"/>
    </Grid>
</ContentPage>